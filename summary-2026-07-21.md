# Progress Summary — 2026-07-21

(Not committed — covered by `.gitignore` `*.md` rule.)

## Resume point (read this first, future me)

I was building the Statum compliance portal end-to-end (DB + ASP.NET WebMethod backend + vanilla JS SPA frontend), per `docs/01-PRD-and-Database-Schema.md` and `docs/03-UIUX-Implementation-Spec.md`. Backend was fully built and smoke-tested via direct API calls — works. Then wired up the frontend and the user tried it in a real browser and reported bugs: buttons refreshing the page, create-agency/create-compliance modals flashing and disappearing, unstructured layout, stray emoji characters.

I diagnosed and fixed 4 real bugs (see "Where we paused" below) directly in `app.js`/`app.css`/`Default.aspx`, rebuilt the backend clean, and syntax-checked `app.js` with Node — but **I have not yet visually confirmed the fixes in an actual browser**. I was setting up a Playwright smoke test (Chromium install finished successfully in the background) to click through the app and verify, when the user interrupted and asked to pause for the day.

**Do this first when resuming:** start IIS Express on the project (`c:\Users\giria\source\repos\ComplianceV2.2\ComplianceV2.2`, port 62743), run the Playwright click-through described in "Next steps", and actually look at the rendered UI before doing anything else — the user's complaint was specifically about things I can't verify from code review alone (layout/structure). Don't assume the 4 fixes are sufficient until seen rendered.

## What's built

**DB (MySQL, local):** `compliance_portal` database, schema in `docs/schema.sql`, seed data in `docs/seed.sql` — already run and loaded. Test users: Master=Ananya(1), Owners=Vikram(2)/Priya(3), Reviewers=Rahul(4)/Sneha(5).

**Backend (ASP.NET Web Forms, .NET 4.7.2, `MySqlConnector` package):**
- `App_Code/Db.cs`, `SessionStore.cs`, `BizLogic.cs`, `Dto.cs`, `Json.cs`
- `ComplianceService.asmx(.cs)` — all WebMethods (DevLogin, GetPlants/Agencies/Compliances/Detail, CreateAgency, CreateCompliance, EditCompliance, MarkComplete, GetNotifications, GetLookups). RBAC enforced server-side, not just UI.
- `Upload.ashx` / `Download.ashx` — file attachments, magic-byte validated (PDF/JPG/PNG), 5MB cap, access-checked on download.
- Web.config: connection string (`compliance_portal` DB, not the `WMS_DB` originally pasted), ScriptService JSON handler wiring, security headers, dev-auth flag.

**Frontend:** `Default.aspx` + `Content/app.css` + `Scripts/app.js` — full SPA per `docs/03-UIUX-Implementation-Spec.md` (Statum navy/brass/ink-stamp design). Dev-login screen simulates the real SSO redirect (real SSO integration deferred — see Open Questions).

All of the above was smoke-tested end-to-end via direct API calls (PowerShell) against IIS Express + MySQL: login, plant/agency/compliance scoping ("visible but locked"), mark-complete + next-due recalculation, and cross-user access blocking all verified working.

## Where we paused

User reported real UI bugs after first browser use:
1. **Fixed** — buttons were submitting the page (`<form runat="server">` wrapped everything in `Default.aspx`, buttons had no `type="button"`, so clicks caused a full page reload). Root-cause fix: removed the `<form runat="server">` wrapper entirely (no ASP.NET server controls are used, so it wasn't needed).
2. **Fixed** — clicking anywhere inside a modal (e.g. an input field) bubbled up to the backdrop and closed the modal instantly. Fixed by only closing on an exact click on the `.overlay` element itself, not bubbled clicks.
3. **Fixed** — emoji glyphs (👤 👁 📁) in the compliance row meta line replaced with the existing inline-SVG icon set for consistency.
4. **Fixed** — modal body flex/scroll CSS, notification panel positioning cleanup, empty-eyebrow guard on page headers.

All fixes applied to `app.js`/`app.css`/`Default.aspx`, backend rebuilt clean (`msbuild` succeeded), `app.js` passed a Node syntax check.

**Not yet done:** a real browser click-through to visually confirm the fixes. Was mid-way through setting up a Playwright smoke test (installed Chromium via `npx playwright install chromium --with-deps` — this finished successfully in the background) when the user paused the session ("we will continue later").

## Next steps (tomorrow)

1. Run the Playwright browser smoke test: dev-login as Master → click through Plant → Agency → Compliance → Detail → open Create Agency modal (type into fields, confirm it stays open) → open Create Compliance modal (same check) → confirm no page reloads anywhere.
2. Visually review spacing/structure once in a real browser — user flagged "elements aren't structured properly" beyond the two hard bugs; need to see it rendered to find what else needs polish.
3. Re-test Owner and Reviewer roles in the browser (locked cards/rows, fulfillment form, read-only reviewer detail view).

## Open questions still pending from user (not blocking, but unresolved)

- Structure/columns of the **external shared DB table** used to resolve the real SSO token → user identity (user said "I'll tell later"). Current `DevLogin` WebMethod is a stand-in; real token-based login endpoint not yet built.
- Whether the incoming SSO token has an expiry/is single-use (user wasn't sure — "token session expire?"). `local_sessions` currently expires after 30 min regardless, which covers this either way, but worth confirming against the real token's own lifetime once known.
- `DevAuthEnabled` flag in `Web.config` must be set to `false` before any real deployment — currently `true` for local dev.

## Key file locations

- Docs: `docs/01-PRD-and-Database-Schema.md`, `docs/03-UIUX-Implementation-Spec.md`, `docs/schema.sql`, `docs/seed.sql`
- Backend: `ComplianceV2.2/App_Code/*.cs`, `ComplianceV2.2/ComplianceService.asmx(.cs)`, `ComplianceV2.2/Upload.ashx(.cs)`, `ComplianceV2.2/Download.ashx(.cs)`
- Frontend: `ComplianceV2.2/Default.aspx`, `ComplianceV2.2/Content/app.css`, `ComplianceV2.2/Scripts/app.js`
- Rules: `claude.md` (project root) — token-light, no planning chatter, HTML/CSS/JS/AJAX/WebMethod only, no ASP controllers, VAPT-clean required, 60-word replies (200 for "discuss").
