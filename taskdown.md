- Agency view: three-dot edit/delete popup now has highest z-index (card-menu-wrap: 150, card-menu: 999) so it renders above all other elements
- Schedule page: Overdue/Due section counts now render as proper colored pill badges (red for overdue, amber for due) instead of plain muted text
- Schedule page: compliance rows redesigned as structured cards with labeled Plant/Agency/Financial Year/Due Date fields, replacing the single-line "plant · agency" text
- Added financialYear to GetMySchedule/ScheduleItemDto to support the richer card display
- Schedule page: compliance cards now have a faint background tint (color-paper-2) so they read as distinct wrappers against the panel
- Owner clicking a Due/Overdue card on Schedule now deep-links with &from=schedule; after marking complete, shows a countdown toast (5→1) and dims/locks the filing form before auto-redirecting back to a fresh Schedule page
- Added serial numbers to each card in the Overdue and Due lists on the Schedule page
- Fixed: the post-fulfillment dim/lock on the filing page was getting wiped instantly by the detail reload; now driven by state so it stays locked for the whole 5s countdown
- Navbar: "Overview | Reports" link replaced with a "Reports" dropdown (Overview, Reports) for master/owner/reviewer
- New Reports page (Reports.aspx/reports.js): the compliance status — detail grid moved out of Overview into its own page, with its own plant switcher (master: all plants, one at a time) and PDF/Excel/Mail export; Overview keeps KPIs, Overdue/Due, owner-summary and recent activity
- Owner/reviewer on Reports see only their own plant (server-side scoped), same as Overview
- Removed underline from the Overview/Reports dropdown menu options
- Fixed: underline persisted due to browser cache — added ?v=2 cache-buster to Content/app.css on all pages
- Reverted: Compliance status — detail grid moved back into Overview (report.js); it does not belong in Reports
- Reports page now starts empty (placeholder "Nothing here yet") — ready for whatever goes there next
- Built the real Reports page: plant selector (master: switch pills; owner/reviewer: fixed "your plant" pill), a clickable list of every compliance in scope, expanding to full info + fulfilment history (scrollable), with per-compliance Download-as-PDF and Send-via-mail export
- Backend: added complianceId to ComplianceStatusRowDto/GetSummaryReport; widened GetComplianceDetail so owner/reviewer can view (read-only) any compliance in a plant they belong to, not just ones they personally own/review
- Shortened the Reports page subtitle
- Reports list: dropped the Due date column, kept Status only
- Reports: clicking a compliance now opens a big centered modal (reusing the app's existing overlay/modal pattern, border-radius'd) instead of expanding the row inline; same info grid, history, and PDF/mail export inside
- Redesigned the per-compliance PDF: neutral dark-graphite header/labels instead of red (red now only used for the Overdue status itself, never decoratively); proper header band, compliance identity block, a label:value details sheet, fulfilment history table, and a footer with page numbers - reads like an official audit record
- Reports modal made 22% bigger (max-width 860px to 1050px); all text/elements inside it (labels, values, pills, history rows, buttons) scaled up 20%, scoped only to this modal
- Reports feature marked done
- Fulfilment history box in the modal now caps at ~7-8 logs tall, scrolls inside for the rest
- Corrected scope: the 7-8-logs-then-scroll cap was actually meant for the compliance detail view in the main app (Default.aspx) - owner's filing page and master/reviewer's read-only history panel - added there too (app.js/app.css)
- Removed horizontal scroll from fulfilment history boxes (overflow-x:hidden)
- Added a browser-tab favicon (shield/checkmark brand mark) to all pages
- PDF and Excel export buttons/menu items now use distinct icons (document+text-lines for PDF, document+grid for Excel) instead of sharing one generic file icon, everywhere exports appear (Overview, Schedule, Reports)
- Fulfilment history scrollbar restyled thin/transparent (no more old-school blocky scrollbar)
- Favicon redone to match the navbar logo exactly: red rounded-square background with white shield/checkmark icon
- Reports modal: fulfilment history now shows latest 10 logs by default, with a small "Latest N / All" dropdown aligned right next to the history header for viewing more
- Reports modal: Download-as-PDF and Send-via-mail buttons moved into a fixed footer at the bottom-left of the modal (no longer scroll away with the content)
- Reports modal: "Current status" now shows the app's ink-stamp component (same one used on the main compliance detail page) instead of the small pill badge
- Reports PDF: added the same ink-stamp visual (double ring, rotated status label) near the top of the document, drawn with jsPDF vector shapes
- Added a glowing "Get training" button in the user account popup, leading to a new Training.aspx "Getting started" page with role-aware guidance (quick-start steps, what you can do per role, Reports/Schedule pointers, status/export FAQ)
- "Get training" button now spans the full width of the user popup, edge to edge
- Training page header now has, aligned right in line with "Getting started": an Export button (anchored PDF/Mail popup exporting the whole guide in official format) and a glowing "Go to SOP" button
- Training Export: PDF renders the getting-started guide as an official document (dark header band, section blocks, numbered steps + bullets, page-numbered footer); Mail copies the same guide as formatted HTML and opens the mail client
- Go to SOP: role-based interactive walkthrough — a full slideshow modal that rebuilds each key app screen as a styled mini-mock (navbar, plants, agencies, compliance detail, overview, reports, schedule, user menu) with a glowing highlight ring on the element being explained, step counter, dots, prev/next + keyboard nav; slides differ per role (owner gets fulfil/schedule, master gets create/recent-activity, reviewer gets read-only)
- Fixed Training SOP: mock "Pick a plant" step now shows locked/inaccessible plant cards for owner/reviewer (matches real app), master still sees both open
- Fixed Training SOP modal height jumping between slides (gave .sop a fixed height)
- Fixed Training SOP highlight ring landing on the wrong spot (was measuring offset from the mock element instead of its actual positioned container)
- Fixed Training PDF export: bullet-list sections (What you can do / Reports & Schedule / Good to know) were printing empty bullets with no text — now prints full content
- Fixed Training mail export: long guides could get silently truncated in the mailto body — now always uses clipboard-copy + paste, with a copy-text prompt fallback so content is never cut
- Training SOP walkthrough rebuilt: every slide now reuses the real app's actual CSS classes/markup shapes (navbar, entity-card, card-grid, detail-head, stamp, panel, crow, sched-card, user-menu, etc.) shrunk to fit via CSS zoom, instead of a separate look-alike "m-*" mock design system — so what's shown is a true miniature of the real screens, not an approximation
- Removed the now-dead .m-* mock CSS block from app.css
- Big architecture change: identity/plant/department moved from local users/departments/plants tables to external plant_master + access schemas (mirroring the real company DBs: plant_master.tbl_plant, tbl_dept, access.login_tokenpass, login_tokenallowed, access.role) — local simulation tables created + seeded for dev since real DBs aren't reachable yet
- compliances.owner_id/reviewer_id (int) -> owner_token/reviewer_token (varchar, references access.login_tokenpass.Token); dropped compliances.department_id entirely (compliance has no dept of its own, only its owner/reviewer do)
- Reviewer is no longer freely picked — Create/Edit Compliance now has a real Plant -> Dept -> Owner/Reviewer cascade, pulling eligible people live from access tables (filtered by app access + Owner/Reviewer role)
- Login (DevLogin) now takes token+role like the real SSO redirect will (querystring-driven on Default.aspx); dropped the old role+userId dev picker backed by hardcoded users
- GetPlants for owner/reviewer is now driven directly by their own PlantID (one person = one plant) instead of scanning compliances - this is what finally makes "owner can't browse other plants" a real, enforced rule
- Fixed a pre-existing bug: editing a compliance never prefilled Owner/Reviewer/Plant/Dept - now it does, using the same new cascade
- Dropped the now-dead local users/departments/plants tables after verifying everything end-to-end
- User menu popup now shows Plant + Department (fetched live via ValidateSession/DevLogin from plant_master/access), structured under the role line
- Redesigned "Get training" button: was a loud full-bleed solid-red pulsing slab, now a softer accent-tinted pill with margin, subtle icon, gentle glow only
- Training SOP: mock screen now shown bigger/more legible (zoom 0.5 -> 0.72, modal enlarged) and removed the redundant last "Your account & training" slide (pointless while already inside training)
- Training SOP: modal size back to original; shrunk stage side padding and bumped zoom (0.72->0.82) so the mock screen fills the space better
- Training SOP: right info panel was mostly empty below short descriptions - added a clickable step outline (all slide titles, current one highlighted) that fills that space and doubles as quick navigation
- Training SOP: widened the modal (1080px -> 1360px) and used that extra width to make the mock screen bigger (zoom 0.82 -> 0.86, max-width 720 -> 800)
- Training SOP: clicking Done now shows a thank-you toast (same style as the fulfilment alert) and logs completion to a new training_completions table (token, plant, completed_at)
- Fixed: owner clicking a compliance from Schedule and then just leaving (not marking complete) landed on the wrong screen (All plants) - Back button now says "Back to Schedule" and actually goes there, when arriving from Schedule
- Compliance detail reached from Schedule now shows just a plain "Back to Schedule" button, no misleading clickable "All plants" breadcrumb trail
- New feature: owner can revert a fulfilment log within 7 days of logging it (only the most recent log, only its own owner) - confirm dialog, then a mandatory 250-char reason, all in one transaction
- Reverting deletes the log + attachments from the app (looks as if never filled) but a full copy (what was filled, what was reverted, why, by whom, when) is permanently kept in a new compliance_log_reverts audit table
- Compliance's due date is correctly restored to what it was before that fulfilment (using the prior log's snapshot, or the original schedule if it was the first-ever log)
- Sends a professional notification email to the reviewer (reviewerToken@mahindra.com) with everything that was originally filed and the revert reason - real SMTP isn't configured yet (Web.config placeholders added), so sends fail gracefully and get logged, they don't block the revert
- Tested: happy path, already-reverted log, reverting a non-latest log, empty reason, wrong owner, reviewer role - all correctly rejected/handled
- Wired real SMTP credentials to come from access.ngpdigital (mail, Password, SmtpServer, SmtpPort) instead of Web.config placeholders - matches the same "external tables drive config" pattern used for plant/dept/people. Table created locally (empty, waiting on real values).
- Fulfilment form now shows a "Projected next due date" (live, updates as completion date changes) so the owner sees the next due date before submitting
- Revert popup: "7 days" and "cannot be undone" now bold in the warning text
- Report page modal no longer shows the compliance's start date
- Schedule page: added an Agency filter dropdown next to the From/To date filters (owner-only page, kept owner-scoped as before)
- Encrypted uploaded attachments at rest (AES-256-CBC, key in Web.config FileEncKey) - VAPT gap-filler for file storage, verified via upload/download round-trip
- Owner completing a compliance now emails the reviewer with a "View Detailed Report" button; link opens new confined ReportView.aspx (login-gated, no nav elsewhere) showing the same detail grid + fulfilment history as the Reports modal, with a PDF download button but no send-via-mail option
- Reworked the report-link auth: click itself is now the login — email links carry a signed, single-use HMAC token (30-day validity, burned on first open via new report_link_used_at column), so no sign-in screen for the real click; reused/expired links fall back to sign-in instead of granting access
- Modularity/optimization pass (no functional change): split ComplianceService.asmx.cs (900 lines) into partial class files by area (Auth/Lookups/Compliances/Fulfilment/Reports); pulled duplicated esc()/fmtDate() out of every page script into Scripts/common.js; fixed N+1 query in GetComplianceDetail (was one attachments query per log, now one batched query). Verified live via IIS Express - login, all split webmethods, and the N+1 fix all confirmed working.
- Fixed download crash (CryptographicException: input data is not a complete block): 4 attachment files predated the AES-256 encryption feature and were still plaintext on disk; download always tried to decrypt, crashing on those. One-time migration encrypted them in place with the existing FileEncKey - no code change, verified via real download/unzip round-trip (valid PDFs recovered).
- Fixed: reverting a fulfilment deleted only the DB row for its attachments, leaving the encrypted file orphaned on disk forever. Now deletes the physical file too, after the DB revert commits (a missing/failed file delete never blocks a successful revert). Verified live: upload -> mark complete -> revert -> file confirmed gone from App_Data/Uploads.
- Added Category to compliance create/edit (master-only, searchable dropdown, fixed list with Other last, License/Test Certification spelling corrected) - server-validated against a whitelist, shown in detail view + Reports modal + confined report page
- Return category gets special due-date math: next due = old due date + frequency (anchored, not done-date based), and fulfillment is blocked once past (old due + frequency) - the filing window for that cycle. Every other category (incl. Renewal) is unchanged: next due = done date + frequency, no restriction on when it's filed. Verified live: blocked a late Return with the exact cutoff in the error, confirmed correct anchored math for an in-window late filing, and confirmed a non-Return category has zero restriction even filed months late.
- Wired the real SSO redirect: Default.aspx?token=<base64>&role=<base64>&method=access now hits a new SsoLogin webmethod (base64-decodes token/role server-side, method is ignored) instead of the DevAuthEnabled-gated DevLogin it was wrongly using before. URL is scrubbed after login so the token doesn't linger in browser history. Verified live: valid base64 token/role logs in correctly, malformed input rejected cleanly (no crash), DevLogin untouched for the local dev screen.
- Added "As and When" category: no frequency at all - master's start date is the first due date directly (create/edit form hides the Frequency field for it), and at each fulfilment the owner types in the next due date themselves (no auto-calc, no restriction on what they pick). Verified live: first due = start date exactly, MarkComplete rejects a missing manual date, and an arbitrary owner-picked date is honored verbatim.
- Added SsoLoginJwt: supports Default.aspx?jwt=<base64 EmpCode|Name|Role|...> SSO login alongside existing token/role querystring
- Verified department dropdown in Create/Edit Compliance already filters by selected plant (GetDepartmentsForPlant uses tbl_dept.Plant_ID) - no change needed
- Added a shared searchable-dropdown helper (ssHtml/syncSsel/showSselList) to Scripts/common.js so every page gets one consistent, app-styled combobox instead of a native select
- Converted the remaining plain selects to the searchable style: Financial Year filter, Create Agency's Plant, dev-login Role/User (app.js + reportview.js), Schedule's Agency filter, and the fulfilment-history "Latest N" limit dropdown (reports.js + reportview.js)
- Session expired/invalid now redirects straight to https://access.mahindra.com (new goToSso() in common.js) across Default, Schedule, Report, Reports, ReportView, Training
- Sign out (Default.aspx user menu, ReportView's Sign out button) now redirects to https://access.mahindra.com instead of showing the local sign-in screen
- Mark-complete button now shows a spinner + "Submitting..." and locks the whole page (.container.is-locked) until the save fully succeeds or fails; unlocks and restores the button on failure so the owner can retry
- Fulfilment form: Completion date and Projected next due date (and the As-and-When manual next due date) now show a small Financial Year chip ("F26" format, same as everywhere else) inline next to the date, in a narrower side-by-side row; chips update live as the date changes
- Training page now shows a status message right under "Getting started": "not completed yet, welcome" for first-timers, or "You have taken this training **N** times, feel free to take it again" (N bold) once completed - new GetTrainingStatus webmethod backs the count, updates live after finishing the SOP
- Reports page (not Overview) now has Financial Year and Owner filter dropdowns (searchable, same style as the rest of the app) above the compliance list - options are derived from the plant's own data; GetSummaryReport/ComplianceStatusRowDto now also return financialYear + ownerToken to support this
- Fixed: GetPeoplePicker matched access.role.LegalCompliance ("Owner"/"Reviewer") case-sensitively - mixed casing in that table could silently misfile or drop people from the Owner/Reviewer picker. Now compared case-insensitively (SQL LOWER() + StringComparison.OrdinalIgnoreCase)
- Fixed: Create/Edit Compliance modal was silently resetting the Agency field whenever Start date, Frequency, or Category was touched afterward - recomputeDuePreview() was wrongly re-fetching and rebuilding the Agency dropdown on every one of those edits, and with nothing marked "selected" for a new compliance it fell back to the first agency in the list. Agency is now only (re)populated on modal open and on Plant change.
- Moved "As and When" out of Category and into the Frequency dropdown (it's a frequency - no fixed interval, owner picks the next due date each time - not a business category). Category list is now just Return/Inspection/Compliance/.../Other. Return can't pick "As and When" as its frequency (its filing-window cutoff needs a real interval) - the option is left out of the dropdown whenever Category is Return, and blocked server-side too (Create/EditCompliance). Updated the 3 backend spots that used to gate on category=="As and When" (Create/EditCompliance next-due calc, MarkComplete, RevertFulfillment's first-log fallback) to gate on frequency_unit=="as_and_when" instead. No existing data to migrate (not yet deployed).
- Bumped ?v= cache-busters on app.css/common.js/app.js/report.js/reports.js/reportview.js/schedule.js/training.js on every page - several were edited multiple times this session without a version bump, so browsers could've kept serving stale cached copies.
- Replaced the "Download N files (.zip)" link in every fulfilment-history list (Default.aspx, Reports modal, ReportView) with a "Files (N)" chip that opens a small menu: Preview or Download zip (same zip as before). Preview opens a new Preview.aspx tab - left sidebar lists every file for that log, right pane shows it inline (PDF in an iframe, JPG/PNG as an image); anything else (not reachable today since uploads are locked to PDF/JPG/PNG, but handled anyway) shows "No preview available" with a direct single-file download button instead. New GetLogFiles webmethod (same owner/reviewer/master access rule as DownloadZip.ashx) backs the file list; actual bytes are streamed by the existing Download.ashx (its mode=preview inline-disposition option was already built but unused until now). Shared filesChipHtml() dropdown lives in common.js so all three history renderers use one implementation.
- Master's Agency cards now have a bottom-right three-dot menu (Edit / Delete). Edit reopens the same Create-agency modal, header changed to "Edit agency", prefilled with its current plant/name/description (new EditAgency webmethod). Delete is a two-step confirmation: step 1 asks "Do you really want to delete the agency X of plant Y?"; step 2 (Cancel/Delete permanently) shows a grid of every compliance under it with its fulfilment count (new GetAgencyDeleteImpact webmethod), then permanently deletes the agency, its compliances, and all their fulfilment logs/attachments/history/reverts/reminders in one DB transaction (new DeleteAgency webmethod) - files on disk are cleaned up after the transaction commits. A snapshot of what was deleted is written to a new delete_log table first (schema below - name changed from the literal "delete" you asked for since that's a reserved SQL word in MySQL and would need backticks everywhere; delete_log avoids that). This is a genuine hard delete with no recovery path other than that log, matching what was asked.

```sql
CREATE TABLE IF NOT EXISTS delete_log (
  delete_id INT PRIMARY KEY AUTO_INCREMENT,
  entity_type VARCHAR(30) NOT NULL,       -- 'agency' for now; room to reuse for other entities later
  entity_id INT NOT NULL,                 -- agency_id at the time of deletion
  entity_name VARCHAR(150) NOT NULL,      -- agency name
  plant_id INT NOT NULL,
  plant_name VARCHAR(150) NOT NULL,       -- snapshot, in case the plant is renamed later
  compliance_count INT NOT NULL,
  fulfillment_count INT NOT NULL,
  details_json JSON,                      -- [{complianceName, fulfillmentCount}, ...] - what was shown to master before they confirmed
  deleted_by VARCHAR(64) NOT NULL,
  deleted_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

- Notification panel scrollbar now matches the fulfilment-history one - thin, transparent track, muted grey thumb, instead of the default white-boxed OS scrollbar
- Files chip: a fulfilment with exactly one attachment now downloads that file directly ("Download file" -> Download.ashx) instead of wrapping it in a .zip; 2+ attachments still zip as before ("Download zip" -> DownloadZip.ashx). Applied everywhere the Files menu appears (Default.aspx, Reports modal, ReportView) and to Preview.aspx's "Download all" button
- Fixed a pre-existing bug: editing a compliance always recomputed next_due_date as Start Date + Frequency (both the modal's live preview and EditCompliance's actual save), completely ignoring any real fulfilment progress - so saving even a trivial edit (fixing the owner, a typo) silently reset an active compliance's due date back near its original start. Now: if neither Frequency nor (for a never-yet-fulfilled compliance) Start Date actually changed, the due date is left untouched entirely. If Frequency does change, it's recomputed anchored to the compliance's last real fulfilment (or Start Date if it has none yet) - never the original start date of an already-active compliance. Frontend preview (app.js) and backend EditCompliance (Compliances.cs) now use the identical rule and the identical "most recent fulfilment" ordering, so what master sees in the modal always matches what actually gets saved.
- Compliance rows (master only) now have the same bottom-right-style three-dot menu as agency cards: Edit / Delete. Edit reopens the same Create-compliance modal ("Edit compliance" header), but Start date and Frequency are now permanently locked (shown as read-only, greyed static fields) once a compliance exists - only name, category, plant/agency, department, owner and reviewer stay editable. EditCompliance webmethod no longer accepts date/frequency changes at all (ignored server-side even if sent), removing the old recompute logic entirely. Delete is the same two-step confirm pattern as agency delete: step 1 asks to confirm, step 2 shows every fulfilment log (date/done-by/remarks) that will be wiped (new GetComplianceDeleteImpact), then permanently deletes the compliance and all its logs/attachments/history/reverts/reminders in one transaction (new DeleteCompliance webmethod), logging a snapshot to the same delete_log table (entity_type='compliance') and cleaning up files on disk after commit.
- Revert-fulfilment submit now shows a spinner + "Reverting..." and locks both the modal and the page (.modal.is-locked, .container.is-locked) until it succeeds or fails - unlocks and restores the button on failure so the owner can retry, matching the same pattern as mark-complete.
- New master-only page: Mail Config (MailConfig.aspx / mailconfig.js, linked from a new navbar "Mail config" button for master). Two tabs - Monday (single Cc list) and Reminders (four independent Cc lists, R1-R4, one per escalation stage) - each tab saves/cancels independently. Recipients are picked via a live name-or-token search across every app-allowed person (any plant, new SearchMailPeople webmethod) or added manually (a raw token/email) when they're not in the directory. New GetMailConfig/SaveMailGroup/GetMailConfigLogs webmethods (ComplianceService.MailConfig.cs); SaveMailGroup diffs the submitted list against what's currently saved and writes the add/remove delta to mail_config_recipient + mail_config_log in one transaction. Page has a "Show recent changes" panel (who added/removed whom, when) backed by the log table.

```sql
CREATE TABLE IF NOT EXISTS mail_config_recipient (
  recipient_id INT PRIMARY KEY AUTO_INCREMENT,
  mail_type VARCHAR(20) NOT NULL,          -- 'monday' | 'reminders'
  group_key VARCHAR(10) NOT NULL,          -- 'cc' (monday) | 'r1'..'r4' (reminders)
  token VARCHAR(64) NOT NULL,              -- access.login_tokenpass.Token, or a manually-typed token/email
  name VARCHAR(150) NOT NULL,
  is_manual TINYINT(1) NOT NULL DEFAULT 0, -- 1 = typed by hand, not picked from the directory search
  added_by VARCHAR(64) NOT NULL,
  added_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uq_mail_recipient (mail_type, group_key, token)
);

CREATE TABLE IF NOT EXISTS mail_config_log (
  log_id INT PRIMARY KEY AUTO_INCREMENT,
  mail_type VARCHAR(20) NOT NULL,
  group_key VARCHAR(10) NOT NULL,
  action VARCHAR(10) NOT NULL,             -- 'add' | 'remove'
  token VARCHAR(64) NOT NULL,
  name VARCHAR(150) NOT NULL,
  is_manual TINYINT(1) NOT NULL DEFAULT 0,
  performed_by VARCHAR(64) NOT NULL,
  performed_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```


- Added MonthlyMailJob: standalone .NET console app (not part of the web app, deployed via Windows Task Scheduler, not Azure) that mails each owner their overdue + due-this-month compliances on the 1st at 8:30 AM, CC to reviewers, using access.ngpdigital SMTP creds and {token}@mahindra.com addressing
- Fixed MonthlyMailJob: owner/reviewer query only checked token existence in access.login_tokenpass, never checked access.role - someone whose Owner/Reviewer role was removed would still get mailed. Now requires access.login_tokenallowed(LegalCompliance=1) + access.role(LegalCompliance='owner'/'reviewer') like the rest of the app.
- Added MondayMailJob: new console app, every Monday 8:35 AM, one mail per plant to that plant's owners+reviewers (role-checked), Cc from that plant's Monday mail-config, body = owner-wise summary grid + compliance status detail grid (no Done Date/Gap columns), same Outlook-safe inline-style design as MonthlyMailJob (860px, header band, greeting, footer)
- Added mail_send_log table (schema.md) - one row per mail actually sent/failed/skipped, used by MonthlyMailJob (per owner) and MondayMailJob (per plant); no run-summary wrapper table, just the per-mail log
- Added ReminderMailJob: new console app, runs daily 9:00 AM, checks every active compliance's R1-R4 reminders (reminders.days_before_due) - eligible once today >= next_due_date - days_before_due (catches up on a missed run), skips if already sent for that compliance+stage+due_date (mail_send_log check, no duplicates on rerun). To = owner+reviewer, Cc = that plant's reminders mail-config for that stage. Extended mail_send_log with compliance_id/reminder_label/due_date columns for this.
- Reports page: modal now closes only via X/close button - removed outside-click (overlay) and Escape-key close handlers (reports.js)
- Fixed: PDF preview showed "refused to connect" - X-Frame-Options was DENY, blocking the app's own same-origin preview iframe; changed to SAMEORIGIN
- Fixed: MailConfig.aspx crashed with parser error "Could not load type" - the page/codebehind/designer files and mailconfig.js were never added to the .csproj, so the codebehind class never compiled into the assembly
- Fixed searchable dropdowns app-wide: clicking one that already has a value now always shows the full option list (was filtering down to just the selected match); typing still filters as before
- Searchable dropdowns app-wide now support keyboard navigation: Up/Down arrows move a highlighted option, Enter selects it, Escape closes the list; bumped app.css/common.js cache-busters
- Added Agency Logo Config (master-only): new "Config" navbar dropdown (Mail config, Agency logo config); new AgencyLogoConfig.aspx with plant→agency picker and PNG upload/replace/remove per agency; new agency_logos table (schema_agency_logo.sql), AgencyLogo.ashx (serves logo to all roles) and AgencyLogoUpload.ashx (master-only, PNG magic-byte + 2MB validated); agency cards app-wide now show the set logo, falling back to the default icon
- Added a proper red border (var(--color-accent)) around any agency's icon-chip/logo-preview once a custom logo is set, replacing the incidental red-tinted shadow that applied to every card regardless of logo; bumped app.css/app.js/agencylogoconfig.js cache-busters
- Reverted the border: uploaded logos now render plain, no border/box-shadow/forced background — just the logo image as-is
- Hovering an agency card that has a logo now shows that logo as a faint (10% opacity) watermark filling the card background; sits behind all text/icons via negative z-index within the card's own stacking context. Bumped app.css/app.js cache-busters
- Slowed the watermark fade-in from 180ms to 500ms so it eases in smoothly instead of popping in abruptly; bumped app.css cache-buster
- Deep audit of the whole app (security + functional) — published as an artifact; found 4 critical/3 high security issues (unsigned role in SSO login, dev-auth bypass shippable to prod, secrets committed to Web.config, debug mode on) plus IDOR-adjacent gaps, a dead filing-window check, and an unbuilt reminder-sender
- Removed the session token from every URL: added an HttpOnly+Secure(when HTTPS)+SameSite=Lax "sid" cookie set at login (SessionCookie.cs), all page guards and file handlers (Download/DownloadZip/AgencyLogo .ashx, every master-only .aspx Page_Load) now resolve it from the cookie first with query-string as fallback; WebMethods' shared RequireSession falls back to the cookie too. Stripped ?sessionId= from every JS-built link/src across app.js, schedule/training/report/reports/reportview/preview/mailconfig/agencylogoconfig.js — nothing changes for the user, but sessionId no longer lands in browser history or server access logs. Bumped every touched script's cache-buster
- Hardened Schedule/Training/Report/Reports pages' showError to also redirect to SSO when the failure message mentions "session" (matching app.js's existing pattern), not just on an HTTP 401 that ASMX never actually sends for a thrown auth exception — closes the one edge case the sessionId-cookie change surfaced (a genuinely expired/missing session on those 4 pages previously showed a dead-end inline error instead of redirecting)
- Fixed Return-category "Projected next due date" preview: `var isReturn = "";` was hardcoded, so the fulfilment form always projected today's date + frequency instead of the existing due date + frequency. Server-side (MarkComplete) was already correct; only the client preview/filing-window-block was wrong. Bumped app.js cache-buster
- Added malware scanning to attachment uploads (Upload.ashx.cs): new App_Code/MalwareScanner.cs shells out to Windows Defender's PowerShell module (Start-MpScan + Get-MpThreatDetection) on a plaintext temp copy before it's ever encrypted/stored; infected files are rejected and logged to audit_log, temp copy always cleaned up. Fails open (upload proceeds) only if Defender itself isn't present on the host - depends on Windows Defender being enabled on the actual deployment box, unverified in this environment
- Fixed the duplicated due-date math (fulfilment "Projected next due date" preview): added BizLogic.ComputeNextDue as the single source of truth, a new read-only PreviewNextDue WebMethod that calls it, and rewired app.js's fulfilment form + completion-date change handler to fetch that preview from the server instead of recomputing locally. MarkComplete now calls the same ComputeNextDue too, so the preview and the actual save can never drift apart again. The separate create/edit-compliance "first due date" preview (recomputeDuePreview, cStart/cFreqNum/cFreqUnit) still duplicates this client-side - out of scope for this fix, flagging for later. Bumped app.js cache-buster
- Fixed Upload.ashx 400 error on every attachment upload: MalwareScanner failed CLOSED (blocked as "infected") whenever the Defender scan itself couldn't run — the common case being the IIS app pool identity lacking rights to invoke Start-MpScan. Now fails OPEN on scan-execution errors (permissions, timeout marker missing, process failure) and logs UPLOAD_SCAN_UNAVAILABLE to audit_log instead of rejecting the upload; a genuine Defender "INFECTED" result still blocks as before.
- Master edit compliance: Frequency is now editable (Start date stays locked); server recomputes next due date + financial year from the last fulfilment (or Start Date if none) and logs the change to compliance history
- Training SOP: master's walkthrough now includes real Create-agency and Create-compliance form mocks (after the + Create step); owner's walkthrough now steps through the fulfilment form field-by-field (completion date/next due, remarks, attachments) before the existing Mark-complete/revert/schedule slides
- Training SOP: master's Create-agency/Create-compliance walkthrough now breaks each field out into its own highlighted slide (Plant, Agency name, Description; then Plant/Agency, Compliance name, Category, Department/Owner, Reviewer, Start date/Frequency, Next due date) with plain-language guidance on what to enter in each
- Fixed file preview bug: Preview.aspx link was missing sessionId param, causing ValidateSession to fail and redirect users to access.mahindra.com. Added sessionId to Preview.aspx links in reports.js, app.js, reportview.js
- Fixed Mail config and Agency logo config links (app.js) missing sessionId, same root cause as preview bug - was kicking users to access.mahindra.com
- Fixed 'Unknown web method GetMailConfig' error: ComplianceService.MailConfig.cs was never added to the .csproj, so mail config WebMethods (GetMailConfig, SaveMailGroup, GetMailConfigLogs, SearchMailPeople) never compiled into the app
